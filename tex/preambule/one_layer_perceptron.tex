\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning}
\begin{document}

\tikzset{%
   neuron missing/.style={
    draw=none, 
    scale=4,
    text height=0.333cm,
    execute at begin node=\color{black}$\vdots$
  },
}

\begin{tikzpicture}[x=1.5cm, y=1.5cm, >=stealth]

\foreach \m/\l [count=\y] in {1,2}
{
 \node [circle,draw,fill=blue!50,minimum size=1cm] (input-\m) at (0,2-\y) {$e_{\l}$};
}
\foreach \m/\l [count=\y] in {3}
{
 \node [circle,draw,fill=blue!50,minimum size=1cm ] (input-\m) at (0,-2) {$e_{n}$};
}
 
 \node [neuron missing]  at (0,-1) {};
 

\foreach \m [count=\y] in {1,2,3}
  \node [circle,draw,fill=green!50,minimum size=1cm ] (hidden-\m) at (4,3-\m) {$P_{\m}$};
  
\foreach \m [count=\y] in {4}
  \node [circle,draw,fill=green!50,minimum size=1cm ] (hidden-\m) at (4,-3) {$P_{k}$};
  
 \node [neuron missing]  at (4,-1.15) {};
 \node [neuron missing]  at (4,-1.9) {};

\foreach \m [count=\y] in {1}
  \node [circle,draw,fill=red!50,minimum size=1cm ] (output-\m) at (8,1.5-\y) {$S_{1}$};
  
\foreach \m [count=\y] in {2}
  \node [circle,draw,fill=red!50,minimum size=1cm ] (output-\m) at (8,-0.5-\y) {$S_{m}$};

 \node [neuron missing]  at (8,-0.45) {};

%\foreach \l [count=\i] in {1,2,3,n}
%  \draw [<-] (input-\i) -- ++(-0.7,0)
%    node [left, left] {$e_{\l}$};

%\foreach \l [count=\i] in {1,k}
%  \node [above] at (hidden-\i.north) {$P_{\l}$};

%\foreach \l [count=\i] in {1,m}
%  \draw [->] (output-\i) -- ++(1,0)
%    node [right] {$S_{ \l}$};

\foreach \i in {1,...,3}
  \foreach \j in {1,...,4}
    \draw [->] (input-\i) -- (hidden-\j);

\foreach \i in {1,...,2}
  \foreach \j in {1,...,4}
    \draw [->] (hidden-\j) -- (output-\i);

%\foreach \l [count=\x from 0] in {Input, Hidden, Ouput}
%  \node [align=center, above] at (\x*2,2) {\l \\ layer};

\end{tikzpicture}

\end{document} 